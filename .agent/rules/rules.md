---
trigger: always_on
---

# Project Rules

## Environment Setup
- **CRITICAL**: All terminal commands MUST run in the `bkdict` environment.
- **Verification**: Before running any script, verify the active environment with `conda env list`.
- **Auto-Fix**: If the wrong environment is active, run `conda activate bkdict`.